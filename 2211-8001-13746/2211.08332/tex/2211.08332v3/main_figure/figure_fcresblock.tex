\begin{figure}[t]
    \centering
    \includegraphics[width=0.48\textwidth]{main_figure/fcresblock.pdf}
    \vspace{-0.6cm}
    \caption{FCResBlock contains two sets of fully connected layers (FC), group normalizations (GN)~\cite{groupnorm}, and sigmoid linear units (SiLU)~\cite{silu}. $x$ is the input text latent code, $t$ is the input time embedding, and $h_i$ are the intermediate features.}
    \label{fig:fcresblock}
\end{figure}
