
% \begin{algorithm}
% \caption{\matchrate}
% \label{alg:matchrate}
% \begin{algorithmic}
% \Require Original dataset $D$, a reconstruction attack $R=\{x_i\}_{i\in [k]}$.
% % \Ensure $y = x^n$
% \State \textbf{Output:} Reconstruction score of $R$. 
% % \State For any row $x\in R_k$.
% \State Let $m$ be the number of rows in $R$.
% % \State Let $\unique{R} $ be the unique set of rows in $R$.
% \State 

% \For{$k = 1,\ldots, m$}
% \begin{align*}
%     \text{rate}(k) \leftarrow \frac{1}{k}\sum_{i=1}^k \indicator{x_i\in D}
% \end{align*}
% \EndFor
% \State\textbf{Output:} $\matchrate(D, R) \leftarrow \frac{1}{m}\sum_{k=1}^m \text{rate}(k)$
% % \State \textbf{Output:} $\left| D \cap R \right| / \left| R \right|$
% \end{algorithmic}
% \end{algorithm}



\begin{algorithm}
\caption{Sampling baseline}
\label{alg:baseline}
\begin{algorithmic}[1]
\Require A sample dataset $S$.
% \State Sort points in $D$ according to their frequency. 
% \State Let $m$ be the number of unique rows in $S$.
% \State Let $F(x)$ denote the frequency of a row $x$ in $S$.

\State \textbf{Output}  $R$, the set of all unique rows of $S$ sorted by frequency.
\end{algorithmic}
\end{algorithm}


