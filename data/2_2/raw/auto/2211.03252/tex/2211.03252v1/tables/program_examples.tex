\begin{table*}[t!]
\centering
\small
\linespread{1}

\setlength{\tabcolsep}{1mm}{

\NewDocumentCommand{\blueback}{ mO{} }{\colorbox{blue!15}{#1}}
\NewDocumentCommand{\greenback}{ mO{} }{\colorbox{green!30}{#1}}


\begin{tabular}{p{17mm}|p{50mm}|p{80mm}}

% \setlength{\arrayrulewidth}{.3em}

\toprule
\textbf{Task} & \textbf{Natural Language Explanation} & \textbf{Interpreted Logical Structure} \\
\midrule

mushroom
&
\textit{\blueback{Foul smelling} are \greenback{Poisonous}. 7 of 7 rows,with no deviation.}
&
% $\text{function}_1$ = \textit{Exist}(\texttt{\blueback{Foul smelling}}; X);
% $\text{function}_2$ = \textit{Exist}(\texttt{\greenback{Poisonous}}; X);
% $\text{function}_3$ = And($\text{function}_1$, $\text{function}_2$; X);
% return $\text{function}_3$(X)
Label$(X)$ = $\texttt{\blueback{Foul smelling}}(X) \land \texttt{\greenback{Poisonous}}(X)$ \\
\\

\hline

indian-liver-patient
&
\textit{Age \blueback{group above 40} \greenback{ensures liver} patient}
&
% $\text{function}_1$ = \textit{Exist}(\texttt{\blueback{group above 40}}; X);
% $\text{function}_2$ = \textit{Exist}(\texttt{\greenback{ensures liver}}; X);
% $\text{function}_3$ = And($\text{function}_1$, $\text{function}_2$; X);
% return $\text{function}_3$(X)
Label$(X)$ = $\texttt{\blueback{group above 40}}(X) \land \texttt{\greenback{ensures liver}}(X)$ \\
\\

\hline

soccer-league-type
&
\textit{If the \blueback{league is W}-PSL then its type is women's soccer}
&
% $\text{function}_1$ = \textit{Exist}(\texttt{\blueback{league is W}}; X);
% return $\text{function}_1$(X) 
Label$(X)$ = $\texttt{\blueback{league is W}}(X)$ \\
\\

\hline

award-nomination-result
&
\textit{If the name of \blueback{association has 'American'} in it then the result was mostly won.}
&
% $\text{function}_1$ = \textit{Exist}(\texttt{\blueback{association has 'American'}}; X);
% return $\text{function}_1$(X)
Label$(X)$ = $\texttt{\blueback{association has 'American'}}(X)$ \\
\\

\hline

dry-bean
&
\textit{Below the 215.00 equivalent diameter leads to the \blueback{Dermos}-an class.}
&
% $\text{function}_1$ = \textit{Exist}(\texttt{\blueback{Dermos}}; X);
% return $\text{function}_1$(X) 
Label$(X)$ = $\texttt{\blueback{Dermos}}(X)$ \\
\\

\bottomrule

\end{tabular}
}

\caption{Examples of interpreted logical structures learned by \model{}. We randomly select 5 tasks from CLUES dataset ~\citep{menon2022clues}, and use the alphabetically first explanation for interpretation. In each logical structure, the words corresponding to the detected concepts are colored in the explanation.
% \xd{for ``interpretability'', is there a ``baseline'' or general human study used in the community? You might want to take a search just to prevent important work is not mentioned.}
}
\label{tab:program_examples}
\end{table*}